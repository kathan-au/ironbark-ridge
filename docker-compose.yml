version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: ironbark-db
    environment:
      POSTGRES_USER: ironbark
      POSTGRES_PASSWORD: ironbark_dev_password
      POSTGRES_DB: ironbark_ridge
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: ironbark-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@ironbark.com
      PGADMIN_DEFAULT_PASSWORD: admin_password
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  pgdata: